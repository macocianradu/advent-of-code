using System.Text;

namespace day21;

public class Robot
{
    public char Pos = 'A';

    public static Dictionary<(char pos, char pad), List<string>> Map = new()
    {
        { ('A', '^'), ["<"] },
        { ('A', 'A'), [""] },
        { ('A', '<'), ["v<<"] },
        { ('A', 'v'), ["v<", "<v"] },
        { ('A', '>'), ["v"] },

        { ('^', '^'), [""] },
        { ('^', 'A'), [">"] },
        { ('^', '<'), ["v<"] },
        { ('^', 'v'), ["v"] },
        { ('^', '>'), [">v", "v>"] },

        { ('<', '^'), [">^"] },
        { ('<', 'A'), [">>^"] },
        { ('<', '<'), [""] },
        { ('<', 'v'), [">"] },
        { ('<', '>'), [">>"] },

        { ('v', '^'), ["^"] },
        { ('v', 'A'), [">^", "^>"] },
        { ('v', '<'), ["<"] },
        { ('v', 'v'), [""] },
        { ('v', '>'), [">"] },

        { ('>', '^'), ["<^", "^<"] },
        { ('>', 'A'), ["^"] },
        { ('>', '<'), ["<<"] },
        { ('>', 'v'), ["<"] },
        { ('>', '>'), [""] }
    };
}
